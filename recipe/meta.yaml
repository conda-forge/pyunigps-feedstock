{% set name = "pyunigps" %}
{% set version = "0.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyunigps-{{ version }}.tar.gz
  sha256: af264141ba93c1e10d5bcdcf6f526583649b9c95f7acf9870faaf535ff420edd

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=75.0.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - pynmeagps >=1.1.0
    - pyrtcm >=1.1.10

test:
  imports:
    - pyunigps
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/semuconsulting/pyunigps
  description: Python library for parsing and generating Unicore UNI GNSS protocol messages.
  summary: Unicore protocol parser and generator
  license: BSD-3-Clause
  license_file: LICENSE
  doc_url: https://www.semuconsulting.com/pyunigps
  dev_url: https://github.com/semuconsulting/pyunigps
extra:
  recipe-maintainers:
    - semuadmin
